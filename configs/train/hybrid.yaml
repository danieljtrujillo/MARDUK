run_name: hybrid_base
seed: 17
output_dir: outputs/runs/hybrid_base
batch_size: 16
eval_batch_size: 16
epochs: 15
learning_rate: 3.0e-4
weight_decay: 0.01
warmup_steps: 500
grad_clip_norm: 1.0
log_every: 20
eval_every: 250
checkpoint_every: 500
fold: 0
device: cuda
